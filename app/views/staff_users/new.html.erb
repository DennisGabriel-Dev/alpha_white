<%# Novo instrutor %>

<%= render "shared/header" %>

<div class="container mx-auto px-4 py-8 max-w-xl">
  <nav class="mb-6">
    <%= link_to staff_users_path, class: "inline-flex items-center text-gray-600 hover:text-primary transition-colors" do %>
      <%= heroicon(:arrow_left, css_class: "w-5 h-5 mr-2") %>
      Voltar para equipe
    <% end %>
  </nav>

  <div class="card">
    <h1 class="text-xl font-bold text-gray-800 mb-6">Novo instrutor</h1>
    <%= render "form", instructor: @instructor %>
  </div>
</div>

<%= render "shared/footer" %>
